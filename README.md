# authentication-server
This repository contains the backend of the authentication system. It is responsible for securely handling user data, authenticating users, and generating tokens for refresh management. The backend is built as a RESTful API, providing endpoints for user registration, login, verification , forgot password and logout.
